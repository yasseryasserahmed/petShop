spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/pet_db
    username: pet_user
    password: pet_pass
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

logging:
  level:
    com.petshop.pet: DEBUG
    org.hibernate.SQL: DEBUG
