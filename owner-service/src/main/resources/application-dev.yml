spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/owner_db
    username: owner_user
    password: owner_pass
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

logging:
  level:
    com.petshop.owner: DEBUG
    org.hibernate.SQL: DEBUG
    org.flywaydb: DEBUG
